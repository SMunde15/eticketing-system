// src/components/AdminPanel/AddAdmin.tsx
import React from 'react';

const AddAdmin: React.FC = () => {
  return (
    <div>
      <h2>Add New Admin</h2>
      <form>
        {/* Add admin form fields */}
        <button type="submit">Add Admin</button>
      </form>
    </div>
  );
};

export default AddAdmin;
